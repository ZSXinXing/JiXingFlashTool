<Window x:Class="JiXingFlashTool.Views.DeviceScreenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JiXingFlashTool.Views" xmlns:viewModel="clr-namespace:JiXingFlashTool.ViewModels" xmlns:hc="https://handyorg.github.io/handycontrol"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize"
        PreviewKeyDown="d3dIS_PreviewKeyDown"
        PreviewKeyUp="d3dIS_PreviewKeyUp"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="{Binding Title}" Height="765" Width="432">
    <Window.DataContext>
        <viewModel:DeviceScreenViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Grid DockPanel.Dock="Bottom"   Height="40" VerticalAlignment="Top" Margin="0" Background="#252525"  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button  Height="28" Grid.Column="0" Command="{Binding BackCommand}">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Image  Source="/./Resource/Image/device_bottom_back.png" />
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button  Width="28" Height="28"  Grid.Column="1" Command="{Binding HomeCommand}">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Image  Source="/./Resource/Image/device_bottom_home.png" />
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button  Width="28" Height="28"  Grid.Column="2" Command="{Binding MenuCommand}">
                <Button.Template>
                    <ControlTemplate>
                        <Grid >
                            <Image  Source="/./Resource/Image/device_botom_more.png" />
                        </Grid>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
        <DockPanel DockPanel.Dock="Top" Height="30" Background="#252525"
                   MouseMove="DockPanel_MouseMove"
                   >
            <Grid DockPanel.Dock="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button  Height="20" Margin="0 0 5 0" Grid.Column="0" Command="{Binding MiniCommand}">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid >
                                <Image  Source="/./Resource/Image/window_mini.png" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button  Height="20" Margin="0 0 5 0" Grid.Column="1"  Command="{Binding CloseCommand}">
                    <Button.Template>
                        <ControlTemplate>
                            <Grid >
                                <Image  Source="/./Resource/Image/ic_close.png" />
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <TextBlock HorizontalAlignment="Left" Margin="10 0 0 0" FontSize="14" Foreground="White" Text="{Binding Title}" Style="{StaticResource TextBlockDefaultBold}"/>
        </DockPanel>
        <Image  x:Name="d3dIS" Source="{Binding OB.D3DImageSource}" Stretch="Fill" Margin="0"
                       PreviewMouseLeftButtonDown="d3dIS_PreviewMouseLeftButtonDown"
                       PreviewMouseMove="d3dIS_PreviewMouseMove"
                       PreviewMouseLeftButtonUp="d3dIS_PreviewMouseLeftButtonUp"
                       PreviewMouseRightButtonDown="d3dIS_PreviewMouseRightButtonDown"
                       PreviewMouseRightButtonUp="d3dIS_PreviewMouseRightButtonUp"
                       PreviewMouseWheel="d3dIS_PreviewMouseWheel"
                       PreviewKeyDown="d3dIS_PreviewKeyDown"
                       PreviewKeyUp="d3dIS_PreviewKeyUp"
                />
    </DockPanel>
</Window>
