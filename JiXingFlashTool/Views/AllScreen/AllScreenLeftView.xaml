<UserControl x:Class="JiXingFlashTool.Views.AllScreen.AllScreenLeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JiXingFlashTool.Views.AllScreen" xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="300"
             xmlns:viewModel="clr-namespace:JiXingFlashTool.ViewModels.AllScreen" xmlns:Converter="clr-namespace:JiXingFlashTool.Converters" 
             Loaded="UserControl_Loaded"
             Width="{Binding SidebarWidth}"
             >

    <UserControl.Resources>
        <Converter:IndexConverter x:Key="IndexConverter"/>
        <Converter:ASLVItemBackgroundConverter x:Key="ASLVItemBackgroundConverter"/>
        <Converter:ASLVItemForegroundConverter x:Key="ASLVItemForegroundConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <viewModel:AllScreenLeftViewModel/>
    </UserControl.DataContext>
    
    <DockPanel>
        <DockPanel.Background>
            <ImageBrush ImageSource="/./Resources/Image/main_left_bg.png"/>
        </DockPanel.Background>
        <!--<StackPanel DockPanel.Dock="Top" Orientation="Vertical"  Height="80" Margin="0 0 0 5">
            --><!--<DockPanel>
                <TextBlock DockPanel.Dock="Left" Text="分组管理" Margin="5 5 0 0" Foreground="#808080" FontSize="16"></TextBlock>
                <Grid></Grid>
            </DockPanel>
            <DockPanel Margin="0 10 0 0">
                <Button DockPanel.Dock="Right" Content="全选" Margin="0 0 5 0"></Button>
                <StackPanel Orientation="Horizontal">
                    <ComboBox Width="100"  SelectedIndex="0" Margin="10 0 10 0" ItemsSource="{Binding CountryModels}" SelectedItem="{Binding SelectCountry}" DisplayMemberPath="Name">
                    </ComboBox>
                    <Button Content="添加" Style="{StaticResource ButtonSuccess}"></Button>
                    <Button Content="删除" Style="{StaticResource ButtonDanger}" Margin="10 0 0 0"></Button>
                </StackPanel>
            </DockPanel>
            <Grid Height="0.5" Background="Gray" Margin="3 10 10 0"></Grid>--><!--
        </StackPanel>-->
        <ListBox ScrollViewer.HorizontalScrollBarVisibility="Hidden"  ItemsSource="{Binding DeviceCollection}" Name="DeviceLB" Background="Transparent">
            <ListBox.Template>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <WrapPanel Orientation="Horizontal" IsItemsHost="True" ScrollViewer.CanContentScroll="True"/>
                    </ScrollViewer>
                </ControlTemplate>
            </ListBox.Template>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <Border Name="DeviceBord" BorderThickness="1" Height="28">
                                    <Grid Background="{Binding ItemBackgroundColor}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"></ColumnDefinition>
                                            <ColumnDefinition Width="260"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Index}" Foreground="{Binding ItemForegroundColor}" VerticalAlignment="Center"  Margin="6 0 0 0"></TextBlock>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="{Binding ItemForegroundColor}" TextAlignment="Left" VerticalAlignment="Center"></TextBlock>
                                        <Grid.InputBindings>
                                            <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding DataContext.ItemDoubleClickCommand,ElementName=DeviceLB}" CommandParameter="{Binding DataContext, ElementName=DeviceBord}"></MouseBinding>
                                            <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.ItemClickCommand,ElementName=DeviceLB}" CommandParameter="{Binding DataContext, ElementName=DeviceBord}"></MouseBinding>
                                        </Grid.InputBindings>
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </DockPanel>
</UserControl>

