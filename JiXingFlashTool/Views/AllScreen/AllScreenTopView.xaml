<UserControl x:Class="JiXingFlashTool.Views.AllScreen.AllScreenTopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JiXingFlashTool.Views.AllScreen"  xmlns:hc="https://handyorg.github.io/handycontrol" xmlns:viewModel="clr-namespace:JiXingFlashTool.ViewModels.AllScreen"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:AllScreenTopViewModel/>
    </UserControl.DataContext>
    <StackPanel  Background="#F6F6F6" Orientation="Horizontal">
        <DockPanel Margin="5 0 0 0">
            <ToggleButton Margin="0,5,0,0" IsChecked="{Binding IsShowSidebar}" Command="{Binding ShowSidebarCommand}" Style="{StaticResource ToggleButtonSwitch}"/>
            <TextBlock Text="侧边栏" Margin="5 4 0 0" VerticalAlignment="Center" HorizontalAlignment="Center"></TextBlock>
        </DockPanel>

        
        <StackPanel Orientation="Horizontal" Margin="10 5 0 0">
            <CheckBox  IsChecked="{Binding IsSelectAll}" Content="全选/全不选" Command="{Binding SelectAllCommand}"/>
            <CheckBox  IsChecked="{Binding IsSyncMouse}" Content="同步鼠标" Command="{Binding SyncMouseCommand}"  Margin="10 0 0 0"/>
            <CheckBox  IsChecked="{Binding IsSyncKeyboard}" Content="同步键盘" Command="{Binding SyncKeyboardCommand}" Margin="10 0 0 0"/>
        </StackPanel>

        <!--<Button Content="同步文件" Margin="10 0 0 0" Style="{StaticResource ButtonPrimary}"></Button>-->
        <Button Content="设置" Margin="10 0 0 0" Style="{StaticResource ButtonPrimary}" Command="{Binding SettingCommand}"></Button>

    </StackPanel>
</UserControl>
