<UserControl x:Class="JiXingFlashTool.Views.SyncFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JiXingFlashTool.Views" xmlns:viewModel="clr-namespace:JiXingFlashTool.ViewModels" xmlns:hc="https://handyorg.github.io/handycontrol" xmlns:Converter="clr-namespace:JiXingFlashTool.Converters"
             mc:Ignorable="d" 
             Height="400" Width="555">
    <UserControl.DataContext>
        <viewModel:SyncFileViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Converter:InverseVisibilityBoolConverter x:Key="InverseVisibilityBoolConverter"/>
        <Converter:VisibilityBoolConverter x:Key="VisibilityBoolConverter"/>
    </UserControl.Resources>

    <Border CornerRadius="4,4,4,4" Background="#ffffff">
        <DockPanel>
            <Border CornerRadius="4 4 0 0 " Background="#2D77FC" Height="40" DockPanel.Dock="Top">
                <DockPanel >
                    <TextBlock Text="传输" Margin="24 9 0 0 "  FontSize="16" Foreground="#FFFFFF" DockPanel.Dock="Left"/>
                </DockPanel>
            </Border>
            <DockPanel DockPanel.Dock="Bottom" Height="45">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button  Content="添加文件" Margin="10 0 0 0" Background="#FFFFFF" Command="{Binding AddFileCommand}"/>
                    <Button  Content="开始" Style="{StaticResource ButtonSuccess}" Command="{Binding StartCommand}" Margin="10 0 0 0"/>
                </StackPanel>
                <Button  Content="关闭" Margin="0 0 10 0" Background="#FFFFFF"  Command="hc:ControlCommands.Close" DockPanel.Dock="Right"/>
                <Grid></Grid>
            </DockPanel>
            <Grid>
                <ListView ScrollViewer.HorizontalScrollBarVisibility="Hidden"  ItemsSource="{Binding FileCollection}" Name="FieLV" Padding="-5">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="#FFFFFF"/>
                            <Setter Property="BorderThickness" Value="0.5"/>
                            <Setter Property="Foreground" Value="#1C1C1C"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="False">
                                    <Setter Property="Background" Value="#FFFFFF" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="IsSelected" Value="True"></Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFFFFF" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="IsSelected" Value="True"></Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.View >
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Background" Value="#2E6CF3"></Setter>
                                    <Setter Property="Foreground" Value="White"></Setter>
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>

                            <GridViewColumn  Header="文件名" Width="200" DisplayMemberBinding="{Binding FileNameWithoutExtension}"/>
                            <GridViewColumn Header="大小" Width="100" DisplayMemberBinding="{Binding Size}"/>
                            <GridViewColumn Header="安装" Width="50">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox  IsChecked="{Binding NeedInstall}" CommandParameter="{Binding ElementName=NeedInstallCkb,Path=DataContext}" Command="{Binding DataContext.NeedInstallCommand,ElementName=FieLV}" Name="NeedInstallCkb" Visibility="{Binding IsApk,Converter={StaticResource VisibilityBoolConverter}}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="优先从手机安装" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding UsePhoneApk}" CommandParameter="{Binding ElementName=UsePhoneApkCkb,Path=DataContext}" Command="{Binding DataContext.UsePhoneApkCommand,ElementName=FieLV}" Name="NeedUsePhoneApkCkb" Visibility="{Binding IsApk,Converter={StaticResource VisibilityBoolConverter}}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="操作" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button FontSize="12" Foreground="OrangeRed"  Style="{StaticResource ButtonCustom}" CommandParameter="{Binding ElementName=DeleteBtn,Path=DataContext}" Command="{Binding DataContext.DeleteCommand,ElementName=FieLV}" Name="DeleteBtn" Content="删除" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </DockPanel>
    </Border>
</UserControl>
