<UserControl x:Class="JiXingFlashTool.Views.SyncFileProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JiXingFlashTool.Views" xmlns:viewModel="clr-namespace:JiXingFlashTool.ViewModels" xmlns:hc="https://handyorg.github.io/handycontrol" xmlns:Converter="clr-namespace:JiXingFlashTool.Converters"
             mc:Ignorable="d" 
             Height="400" Width="600">
    <UserControl.DataContext>
        <viewModel:SyncFileProgressViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Style/SyncFileProgressStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border CornerRadius="4,4,4,4" Background="#ffffff">
        <DockPanel>
            <Border CornerRadius="4 4 0 0 " Background="#2D77FC" Height="40" DockPanel.Dock="Top">
                <DockPanel >
                    <TextBlock Text="传输进度" Margin="24 9 0 0 "  FontSize="16" Foreground="#FFFFFF" DockPanel.Dock="Left"/>
                </DockPanel>
            </Border>
            <DockPanel DockPanel.Dock="Bottom" Height="45">
                <Button Style="{StaticResource ButtonSuccess}"  Content="完成" Margin="0 0 10 0"  Command="{Binding FinishCommand}" DockPanel.Dock="Right" Visibility="{Binding FinishSync,Converter={StaticResource VisibilityBoolConverter}}"/>
                <Button Style="{StaticResource ButtonDanger}"  Content="停止" Margin="0 0 10 0"  Command="{Binding CloseCommand}" DockPanel.Dock="Right"/>
                <Grid></Grid>
            </DockPanel>
            <Grid>
                <ListView ScrollViewer.HorizontalScrollBarVisibility="Hidden"  ItemsSource="{Binding DeviceCollection}" Name="FieLV" Padding="-5">


                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Height" Value="30" />
                            <Setter Property="Background" Value="#FFFFFF"/>
                            <Setter Property="BorderThickness" Value="0.5"/>
                            <Setter Property="Foreground" Value="#1C1C1C"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="False">
                                    <Setter Property="Background" Value="#FFFFFF" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="IsSelected" Value="True"></Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFFFFF" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="IsSelected" Value="True"></Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.View >
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Background" Value="#2E6CF3"></Setter>
                                    <Setter Property="Foreground" Value="White"></Setter>
                                    <Setter Property="IsHitTestVisible" Value="False"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>

                            <GridViewColumn  Header="IP" Width="100" DisplayMemberBinding="{Binding IP}"/>
                            <GridViewColumn Header="总数" Width="100" DisplayMemberBinding="{Binding SumCount}"/>
                            <GridViewColumn Header="进度" Width="150" DisplayMemberBinding="{Binding Progress}"/>
                            <GridViewColumn Header="成功" Width="125" DisplayMemberBinding="{Binding SuccessCount}"/>
                            <GridViewColumn Header="失败" Width="125" DisplayMemberBinding="{Binding FailureCount}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </DockPanel>
    </Border>
</UserControl>
